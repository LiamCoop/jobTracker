export interface Todo {
  id: string;
  created: string;
  text: string;
  completed: boolean;
}

export interface jobPosting {
  id: string;
  createdAt: string;
  applied: boolean;
  company: string;
  title: string;
  description: string;
  contact: string;
  notes: string;
}
